import { BrowserRouter, Routes } from "react-router-dom";

export default function App() {

    return (
        <BrowserRouter>
            <main>
                <Routes>
                </Routes>
            </main>
        </BrowserRouter>
    )
}